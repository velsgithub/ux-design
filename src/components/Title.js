export const StyledH4 = props => {
    return <h4 className={props.className} style={{ color: props.color }}>{props.label}</h4>
}

export const StyledSpan = props => {
    return <span className={props.className} style={{ color: props.color }}>{props.label}</span>
}


export const StyledH6 = props => {
    return <h6 className={props.className} style={{ color: props.color }}>{props.label}</h6>
}
